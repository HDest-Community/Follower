extend class HDFollower
{
	virtual void GiveStartingItems()
	{
		Medikits = hdf_loadout_medikits;
		Stimpacks = hdf_loadout_stimpacks + Medikits;

		int ArmorType = hdf_loadout_armor;
		if (ArmorType > 0)
		{
			GiveArmour(1.0, ArmorType >= 2 ? 1.0 : 0.0, 1.0);
		}
	}
}